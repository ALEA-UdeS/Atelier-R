library(xaringan)
inf_mr()
library(xaringan)
inf_mr()
library(xaringan)
inf_mr()
#utility functions
readinteger <- function()
{
n <- readline(prompt="Enter an integer: ")
if(!grepl("^[0-9]+$",n))
{
return(readinteger())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("Guess a number between 0 and 100.\n")
while(guess != num)
{
guess <- readinteger()
if (guess == num)
{
cat("Congratulations,", num, "is right.\n")
}
else if (guess < num)
{
cat("It's bigger!\n")
}
else if(guess > num)
{
cat("It's smaller!\n")
}
}
#utility functions
LireNombre <- function()
{
n <- readline(prompt="Écrivez un nombre entier ")
if(!grepl("^[0-9]+$",n))
{
return(readinteger())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("L'ordinateur à choisi un nombre entre 0 et 100 que vous devez deviner.\n")
car("Choisissez un nombre entre 0 et 100. \n")
while(guess != num)
{
guess <- readinteger()
if (guess == num)
{
cat("Félicitations,", num, "est le nombre que l'ordinateur avait choisit!\n")
}
else if (guess < num)
{
cat("Le nombre choisi par l'ordinateur est plus grand", guess, "!\n")
}
else if(guess > num)
{
cat("Le nombre choisi par l'ordinateur est plus petit que", guess, "!\n")
}
}
#utility functions
LireNombre <- function()
{
n <- readline(prompt="Écrivez un nombre entier ")
if(!grepl("^[0-9]+$",n))
{
return(readinteger())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("L'ordinateur à choisi un nombre entre 0 et 100 que vous devez deviner.\n")
cat("Choisissez un nombre entre 0 et 100. \n")
while(guess != num)
{
guess <- readinteger()
if (guess == num)
{
cat("Félicitations,", num, "est le nombre que l'ordinateur avait choisit!\n")
}
else if (guess < num)
{
cat("Le nombre choisi par l'ordinateur est plus grand", guess, "!\n")
}
else if(guess > num)
{
cat("Le nombre choisi par l'ordinateur est plus petit que", guess, "!\n")
}
}
#utility functions
LireNombre <- function()
{
n <- readline(prompt="Écrivez un nombre entier ")
if(!grepl("^[0-9]+$",n))
{
return(LireNombre())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("L'ordinateur à choisi un nombre entre 0 et 100 que vous devez deviner.\n")
cat("Choisissez un nombre entre 0 et 100. \n")
while(guess != num)
{
guess <- LireNombre()
if (guess == num)
{
cat("Félicitations,", num, "est le nombre que l'ordinateur avait choisit!\n")
}
else if (guess < num)
{
cat("Le nombre choisi par l'ordinateur est plus grand", guess, "!\n")
}
else if(guess > num)
{
cat("Le nombre choisi par l'ordinateur est plus petit que", guess, "!\n")
}
}
#utility functions
readinteger <- function()
{
n <- readline(prompt="Enter an integer: ")
if(!grepl("^[0-9]+$",n))
{
return(readinteger())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("Guess a number between 0 and 100.\n")
while(guess != num)
{
guess <- readinteger()
if (guess == num)
{
cat("Congratulations,", num, "is right.\n")
}
else if (guess < num)
{
cat("It's bigger!\n")
}
else if(guess > num)
{
cat("It's smaller!\n")
}
}
#utility functions
LireNombre <- function()
{
n <- readline(prompt="Écrivez un nombre entier ")
if(!grepl("^[0-9]+$",n))
{
return(LireNombre())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("L'ordinateur à choisi un nombre entre 0 et 100 que vous devez deviner.\n")
cat("Choisissez un nombre entre 0 et 100. \n")
while(guess != num)
{
guess <- LireNombre()
if (guess == num)
{
cat("Félicitations,", num, "est le nombre que l'ordinateur avait choisit!\n")
}
else if (guess < num)
{
cat("Le nombre choisi par l'ordinateur est plus grand", guess, "!\n")
}
else if(guess > num)
{
cat("Le nombre choisi par l'ordinateur est plus petit que", guess, "!\n")
}
}
LireNombre <- function()
{
n <- readline(prompt="Écrivez un nombre entier:")
if(!grepl("^[0-9]+$",n))
{
return(LireNombre())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("L'ordinateur à choisi un nombre entre 0 et 100 que vous devez deviner.\n")
cat("Choisissez un nombre entre 0 et 100. \n")
while(guess != num)
{
guess <- LireNombre()
if (guess == num)
{
cat("Félicitations,", num, "est le nombre que l'ordinateur avait choisit!\n")
}
else if (guess < num)
{
cat("Le nombre choisi par l'ordinateur est plus grand que", guess, "!\n")
}
else if(guess > num)
{
cat("Le nombre choisi par l'ordinateur est plus petit que", guess, "!\n")
}
}
LireNombre <- function()
{
n <- readline(prompt="Écrivez un nombre entier:")
if(!grepl("^[0-9]+$",n))
{
return(LireNombre())
}
return(as.integer(n))
}
# real program start here
num <- round(runif(1) * 100, digits = 0)
guess <- -1
cat("L'ordinateur à choisi un nombre entre 0 et 100 que vous devez deviner.\n")
cat("Choisissez un nombre entre 0 et 100. \n")
while(guess != num)
{
guess <- LireNombre()
if (guess == num)
{
cat("Félicitations,", num, "est le nombre que l'ordinateur avait choisit!\n")
}
else if (guess < num)
{
cat("Le nombre choisi par l'ordinateur est plus grand que", guess, "!\n")
}
else if(guess > num)
{
cat("Le nombre choisi par l'ordinateur est plus petit que", guess, "!\n")
}
}
Coding
